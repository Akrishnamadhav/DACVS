Flask==3.0.3
Flask-Cors==4.0.0
# Pillow 10.4.0 fixes KeyError: '__version__' build failure on Render (Python 3.11)
Pillow==10.4.0
numpy==1.26.4

# OCR stack — CPU-only. Python version is pinned to 3.11 via runtime.txt
# torch 2.2.2 only supports Python 3.8–3.11
torch==2.2.2
torchvision==0.17.2
torchaudio==2.2.2
easyocr==1.7.1
opencv-python-headless==4.10.0.84
PyMuPDF==1.24.9

# Production WSGI server (required for Render deployment)
gunicorn==21.2.0
